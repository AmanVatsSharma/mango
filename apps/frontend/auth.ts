// auth.ts
/**
 * File:        apps/frontend/auth.ts
 * Module:      NextAuth server-side stub
 * Purpose:     Provides the NextAuth `auth()` server-side helper as a stub.
 *              The actual auth logic is handled by our AuthProvider + JWT interceptor.
 *
 * Author:      Mango Nx Workspace
 * Last-updated: 2026-05-18
 */

export async function auth() {
  // Server-side auth stub — actual auth handled by AuthProvider on the client
  return null
}